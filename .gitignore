# Data — never commit raw health data publicly
data/raw/

# Trained model — can be regenerated by running main.py
models/

# Virtual environment — too large, machine-specific
.venv/

# Python cache files — auto-generated, not source code
__pycache__/
*.pyc
.pytest_cache/

# macOS system file — not part of your project
.DS_Store